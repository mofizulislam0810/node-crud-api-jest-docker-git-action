services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://username:password@cluster0.9xqnckg.mongodb.net/myFirstDatabase
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
